import { Colors } from "./Colors";

type IconPropstype = {
  width: number;
  height: number;
  color: string;
};

export const Icon: IconPropstype = {
  height: 18,
  width: 18,
  color: Colors.light.grayed,
};
